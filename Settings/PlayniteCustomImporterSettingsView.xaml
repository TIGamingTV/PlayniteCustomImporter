<UserControl x:Class="PlayniteCustomImporter.Settings.PlayniteCustomImporterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Source folder"
                       FontWeight="Bold"
                       Margin="0,0,0,4" />
            <TextBlock Text="Games are imported from this folder. The Import button starts browsing here."
                       TextWrapping="Wrap"
                       Opacity="0.7"
                       Margin="0,0,0,6" />
            <DockPanel Margin="0,0,0,20">
                <Button DockPanel.Dock="Right"
                        Content="Browse..."
                        MinWidth="90"
                        Margin="8,0,0,0"
                        Command="{Binding BrowseSourceFolderCommand}" />
                <TextBox Text="{Binding Settings.SourceFolder, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" />
            </DockPanel>

            <DockPanel Margin="0,0,0,4">
                <Button DockPanel.Dock="Right"
                        Content="Add location"
                        MinWidth="110"
                        Command="{Binding AddStorageLocationCommand}" />
                <TextBlock Text="Storage locations"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />
            </DockPanel>
            <TextBlock Text="When importing, the selected game folder is moved into one of these locations."
                       TextWrapping="Wrap"
                       Opacity="0.7"
                       Margin="0,0,0,6" />

            <ItemsControl ItemsSource="{Binding Settings.StorageLocations}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Margin="0,0,6,0"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Display name" />
                            <TextBox Grid.Column="1"
                                     Margin="0,0,6,0"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Folder path" />
                            <Button Grid.Column="2"
                                    Content="Browse..."
                                    MinWidth="80"
                                    Margin="0,0,6,0"
                                    Command="{Binding DataContext.BrowseStorageLocationCommand,
                                        RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Grid.Column="3"
                                    Content="Remove"
                                    MinWidth="70"
                                    Command="{Binding DataContext.RemoveStorageLocationCommand,
                                        RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
